name: CausalJudge Data Integrity Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python 3.10
      uses: actions/setup-python@v5
      with:
        python-version: "3.10"
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pandas numpy scikit-learn openai
        # Install other requirements if requirements.txt exists
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
    
    - name: Run data integrity tests
      run: |
        echo "🔍 Running CausalJudge data integrity tests..."
        echo "=============================================="
        echo "ℹ️  NOTE: No OpenAI API key required - testing data files only"
        echo ""
        
        # Run the test setup and capture output
        if python scripts/test_setup.py 2>&1 | tee /tmp/test_output | grep -q "4/5 tests passed\|5/5 tests passed"; then
          echo "✅ All critical tests passed!"
        else
          echo "❌ Critical tests failed!"
          cat /tmp/test_output
          exit 1
        fi
      env:
        CHEN_OPENAI_API_KEY: ""
        OPENAI_API_KEY: ""
    
    - name: Test import structure
      run: |
        echo "🔍 Testing import structure..."
        python -c "
        import sys
        import os
        sys.path.insert(0, 'src')
        from causal_evaluation import CausalEvaluationSystem
        print('✅ Core imports work correctly')
        
        # Test that system initializes without API key
        system = CausalEvaluationSystem('dummy', 'gpt-4o-mini')
        print('✅ System initialization works without API key')
        "
    
    - name: Test scripts can run
      run: |
        echo "🔍 Testing script help commands..."
        python scripts/run_evaluation.py --help
        python scripts/run_single_evaluation.py --help  
        python scripts/run_multi_evaluation.py --help
        echo "✅ All script help commands work"